# DEPRECATED: Dependencies are now managed in pyproject.toml
#
# To install dependencies, use:
#   pip install -e .                    # Install main dependencies
#   pip install -e ".[test]"            # Install with test dependencies
#   pip install -e ".[dev]"             # Install with dev dependencies
#   pip install -e ".[test,dev]"        # Install with test and dev dependencies
#   pip install -e ".[production]"      # Install with production server dependencies
#
# This file is kept for reference only.
# See pyproject.toml [project.dependencies] and [project.optional-dependencies]

# Core dependencies - NOW IN pyproject.toml [project.dependencies]
# pydantic>=2.12.0
# numpy-financial>=1.0.0
# pandas>=2.3.0
# numpy>=2.3.0
# streamlit>=1.52.0
# plotly>=6.5.0
# google-auth>=2.43.0
# google-auth-oauthlib>=1.2.2
# google-auth-httplib2>=0.2.1
# gspread>=6.2.1
# streamlit-authenticator>=0.4.2
# streamlit-cookies-manager>=0.2.0,<0.3.0
# openpyxl>=3.1.0
# scipy>=1.16.0
# click>=8.3.0
# toml>=0.10.0

# Testing dependencies - NOW IN pyproject.toml [project.optional-dependencies.test]
# pytest>=7.4.0
# pytest-playwright>=0.4.3
# playwright>=1.40.0

# Development dependencies - NOW IN pyproject.toml [project.optional-dependencies.dev]
# jupyter>=1.0.0
# jupyterlab>=4.0.0
# matplotlib>=3.10.0

# Production server - NOW IN pyproject.toml [project.optional-dependencies.production]
# gunicorn>=20.1.0
# uvicorn>=0.18.0
